export type User = {
    id?: string;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};

export type UserDto = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};